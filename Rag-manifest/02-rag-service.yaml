apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: rag
spec:
  selector:
    app: rag-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  