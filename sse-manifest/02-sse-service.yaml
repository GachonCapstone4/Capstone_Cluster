apiVersion: v1
kind: Service
metadata:
  name: sse-service
  namespace: sse-service
spec:
  selector:
    app: sse-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  