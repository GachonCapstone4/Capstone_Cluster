apiVersion: v1
kind: Service
metadata:
  name: bigdata-service
  namespace: bigdata
spec:
  type: ClusterIP
  selector:
    app: bigdata
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
